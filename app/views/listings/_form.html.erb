<%= form_with(model: listing, local: true) do |form| %>
  <% if listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
      <% listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.select(:category, [["Face", "face"], ["Eyes", "eyes"], ["Lips", "lips"], ["Accessories", "accessories"]]) %>
  </div>

  <div class="field">
    <%= form.label :price, "Price $" %>
    <%= form.number_field :price, min: 0 %>
  </div>


  <%= form.fields_for :location do |l| %>
    <div class="field"> 
      <%= l.label :suburb %>
      <%= l.text_field :suburb %>
    </div>

    <div class="field"> 
      <%= l.label :postcode %>
      <%= l.text_field :postcode %>
    </div>

    <div class="field"> 
      <%= l.label :state %>
      <%= l.text_field :state %>
    </div>
  <% end %>

  
  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
      <%= form.label :pic, "Upload picture" %>
      <%= form.file_field :pic, accept: "image/png, image/gif, image/jpeg, image/jpg, image/JPG" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
