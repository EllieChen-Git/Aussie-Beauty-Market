<!-- Simple search button: case insensitive, simple fuzzy search -->
<%= form_tag(listings_path, method: :get) do %>
    <%= label_tag(:search, "Please enter keywords:") %>
    <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag "Search" %>
<% end %>