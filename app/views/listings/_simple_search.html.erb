<!-- Simple search form: case insensitive, simple fuzzy search -->
<%= form_tag(listings_path, method: :get) do %>
    <%= label_tag(:search, "Search makeups:") %>
    <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag "Go!", :name => nil %>
<% end %>